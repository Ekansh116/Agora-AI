DATABASE_URL=sqlite:///./community_intelligence.db
CHROMA_DB_DIR=./chroma_db
OLLAMA_BASE_URL=http://localhost:11434
LLM_MODEL=qwen2.5:1.5b
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2

# SSL verification setting (Leave as 'false' for normal TLS validation. Set to 'true' only if behind a corporate proxy requiring custom CAs)
DISABLE_SSL_VERIFY=false

PORT=8000
HOST=0.0.0.0
